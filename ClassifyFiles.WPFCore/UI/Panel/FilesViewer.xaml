<UserControl x:Class="ClassifyFiles.UI.Panel.FilesViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClassifyFiles.UI.Panel"
             mc:Ignorable="d" 
                  xmlns:component="clr-namespace:ClassifyFiles.UI.Component"
    xmlns:ui="clr-namespace:ClassifyFiles.UI"
                 xmlns:m="http://schemas.modernwpf.com/2019"

             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ui:NotFirst2VisibilityConverter x:Key="i2v"/>
        <ui:IsNotNull2BoolConverter x:Key="nn2b"/>
        <ui:IsNotNull2VisibilityConverter x:Key="nn2v"/>
        <DataTemplate x:Key="grdIconView">
            <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="2">
                <DockPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                                      >
                    <DockPanel.ToolTip>
                        <ToolTip>
                            <m:SimpleStackPanel Spacing="8">
                                <TextBlock Text="{Binding DisplayDir}"/>
                                <TextBlock FontWeight="Bold" Text="{Binding DisplayName}"/>
                            </m:SimpleStackPanel>
                        </ToolTip>
                    </DockPanel.ToolTip>
                    <TextBlock DockPanel.Dock="Bottom" FontSize="12" Text="{Binding Path=DisplayName, Mode=OneTime}"
                               Margin="0,8,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" Height="32" MaxWidth="{Binding LargeIconSize}"/>
                    <component:FileIcon File="{Binding .}" UseLargeIcon="True"/>
                </DockPanel>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="grdTileView">
            <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="4,8">
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center"  >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="144"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Column="2" Text="{Binding Path=DisplayName, Mode=OneTime}"
                               Margin="0,8,0,0" HorizontalAlignment="Left" TextWrapping="Wrap" MaxHeight="{Binding LargeIconSize}"/>
                    <TextBlock Grid.Column="2" Grid.Row="2" Text="{Binding DisplayDir,Mode=OneTime}"
                               Foreground="Gray" TextWrapping="Wrap" FontSize="11" MaxHeight="{Binding SmallIconSize}"/>
                    <component:TagGroup Grid.Column="4" File="{Binding .}" Orientation="Vertical" 
                                        Grid.RowSpan="99" MaxHeight="{Binding LargeIconSize}" TagMouseDown="Tags_MouseDown"/>
                    <component:FileIcon File="{Binding .}" UseLargeIcon="True" Grid.RowSpan="99" />
                </Grid>
            </Border>
        </DataTemplate>

        <ContextMenu x:Key="menu" Opened="ContextMenu_Opened">
        </ContextMenu>
    </UserControl.Resources>
    <Grid  Margin="8,4,4,4" PreviewMouseWheel="ListBox_PreviewMouseWheel" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <m:AutoSuggestBox
                    Grid.Column="0"
                        x:Name="txtSearch"
                        PlaceholderText="{Binding Files.Count, StringFormat={}文件总数：{0:}}"
                        TextChanged="SearchTextBox_TextChanged"
                        QueryIcon="Find"
                        QuerySubmitted="SearchTextBox_QuerySubmitted"
                        SuggestionChosen="SearchTextBox_SuggestionChosen" 
                    VerticalAlignment="Center"/>
            <Grid x:Name="grdAppBar" Grid.Column="4" HorizontalAlignment="Right">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <m:AppBarToggleButton Grid.Column="3"  Label="列表视图"  Click="ViewTypeButton_Click" Tag="1" IsChecked="True">
                    <m:AppBarToggleButton.Icon>
                        <m:FontIcon
                                    FontFamily="Segoe MDL2 Assets"
                                    Glyph="&#xE8FD;" />
                    </m:AppBarToggleButton.Icon>
                </m:AppBarToggleButton>
                <m:AppBarToggleButton Grid.Column="4" Label="图标视图" Click="ViewTypeButton_Click" Tag="2">
                    <m:AppBarToggleButton.Icon>
                        <m:FontIcon
                                    FontFamily="Segoe MDL2 Assets"
                                    Glyph="&#xF0E2;" />
                    </m:AppBarToggleButton.Icon>
                </m:AppBarToggleButton>  
                <m:AppBarToggleButton Grid.Column="5" Label="平铺视图" Click="ViewTypeButton_Click" Tag="3">
                    <m:AppBarToggleButton.Icon>
                        <m:FontIcon
                                    FontFamily="Segoe MDL2 Assets"
                                    Glyph="&#xE8BC;" />
                    </m:AppBarToggleButton.Icon>
                </m:AppBarToggleButton>
                <m:AppBarToggleButton Grid.Column="6"  Label="树型视图"  Click="ViewTypeButton_Click" Tag="4">
                    <m:AppBarToggleButton.Icon>
                        <m:FontIcon
                                    FontFamily="Segoe MDL2 Assets"
                                    Glyph="&#xED25;" />
                    </m:AppBarToggleButton.Icon>
                </m:AppBarToggleButton>
                <TextBlock
                        x:Name="Control5Output"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center" />
            </Grid>
        </Grid>

        <Grid Grid.Row="4" 
                        Visibility="Collapsed" x:Name="grdFilesArea">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <m:GridView     x:Name="lbxGrdFiles" ContextMenu="{StaticResource menu}"
                      PreviewMouseDoubleClick="lvwFiles_PreviewMouseDoubleClick" 
                         ItemsSource="{Binding Path=PagingFiles}" />
            <ScrollViewer x:Name="scrPagging" Grid.Row="2"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="60" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                <WrapPanel x:Name="stkPagging" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
        
        
        
        <Grid Grid.Row="4" x:Name="lvwFilesArea">
            <Grid.RowDefinitions >
                <RowDefinition Height="*"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ListView   ItemsSource="{Binding Files}" x:Name="lvwFiles" SelectionMode="Extended"
                    Loaded="lvwFiles_Loaded"  PreviewMouseDoubleClick="lvwFiles_PreviewMouseDoubleClick"
                        ContextMenu="{StaticResource menu}"   >
                <ListView.ItemContainerStyle >
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4,24,4"  >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="4"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <component:FileIcon File="{Binding .}" Grid.RowSpan="99"/>
                            <TextBlock Text="{Binding DisplayName}" Grid.Column="2" Grid.Row="0"/>
                            <TextBlock Text="{Binding DisplayDir}" Grid.Column="2" Grid.Row="2" Opacity="0.5"/>
                            <!--横向的标签列表-->
                            <component:TagGroup Grid.Column="4" Grid.RowSpan="99" File="{Binding .}" TagMouseDown="Tags_MouseDown"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <TreeView Name="treeFiles" Visibility="Collapsed"   ContextMenu="{StaticResource menu}" PreviewMouseRightButtonDown="treeFiles_PreviewMouseRightButtonDown"
                                          Grid.Row="4"  ItemsSource="{Binding FileTree}" PreviewMouseDoubleClick="lvwFiles_PreviewMouseDoubleClick"
                  TreeViewItem.Expanded="TreeItems_Expanded">

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate  ItemsSource="{Binding SubFiles}">
                    <StackPanel Orientation="Horizontal">
                        <component:FileIcon File="{Binding .}"/>
                        <TextBlock VerticalAlignment="Center" FontSize="12" Margin="12,0,0,0" Text="{Binding DisplayName}"/>
                        <component:TagGroup Margin="24,0,0,0" File="{Binding .}" TagMouseDown="Tags_MouseDown"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

    </Grid>
</UserControl>
