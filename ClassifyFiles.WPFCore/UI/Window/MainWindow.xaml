<local:WindowBase
    x:Class="ClassifyFiles.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"             
    xmlns:mde="clr-namespace:MaterialDesignExtensions.Controls;assembly=MaterialDesignExtensions"

        xmlns:wpw="clr-namespace:ClassifyFiles;assembly=ClassifyFiles.Core"
        xmlns:local="clr-namespace:ClassifyFiles.UI"
         xmlns:glo="clr-namespace:System.Globalization;assembly=System.Runtime"
xmlns:panel="clr-namespace:ClassifyFiles.UI.Panel"
    Loaded="Window_Loaded"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
    mc:Ignorable="d"
    Closing="Window_Closing"
    WindowStartupLocation="CenterScreen"
        Title="文件分类浏览器" Height="600" Width="1200" x:Name="win">
    <Grid x:Name="outGrd" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <Grid MinWidth="220" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ToggleButton 
                        Style="{StaticResource MaterialDesignHamburgerToggleButton}" 
                        DockPanel.Dock="Top"  HorizontalAlignment="Right" 
                        Margin="16" IsChecked="{Binding ElementName=MenuToggleButton, Path=IsChecked, Mode=TwoWay}" />
                    <TextBlock Grid.Row="0" Margin="16,0,0,0" FontSize="24" HorizontalAlignment="Left" VerticalAlignment="Center">
                        项目
                    </TextBlock>
                    <ListBox 
                        Margin="0 16 0 16"
                        SelectedIndex="0"
                        Grid.Row="2"
                        SelectedItem="{Binding SelectedProject}"
                        ItemsSource="{Binding Projects}">
                        <ListBox.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource MaterialDesignScrollBarMinimal}"/>
                        </ListBox.Resources>
                        <ListBox.ItemTemplate>
                            <DataTemplate >
                                <TextBlock Text="{Binding Name}" Margin="32 0" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button Grid.Row="4" Click="AddProjectButton_Click" 
                            Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                        <materialDesign:PackIcon Kind="Add"/>
                    </Button>
                </Grid>
            </materialDesign:DrawerHost.LeftDrawerContent>
            <DockPanel>
                <materialDesign:ColorZone Padding="8,4" materialDesign:ShadowAssist.ShadowDepth="Depth2"
                                            Mode="PrimaryMid" DockPanel.Dock="Top">
                    <DockPanel>
                        <ToggleButton
                            x:Name="MenuToggleButton"
                            Style="{StaticResource MaterialDesignHamburgerToggleButton}"
                            IsChecked="False"
                            Click="MenuToggleButton_OnClick"/>
                        <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges" StaysOpen="False">
                            <StackPanel>

                                <Button  Click="Button_Click">设置</Button>
                                <Separator/>
                                <Button Content="Goodbye" />
                            </StackPanel>
                        </materialDesign:PopupBox>
                        <StackPanel    Orientation="Horizontal" DockPanel.Dock="Right"    Margin="2,2,16,2">
                            <RadioButton      Style="{StaticResource MaterialDesignTabRadioButton}" Margin="4" IsChecked="True" Content="浏览" 
                                             Tag="1" Checked="RadioButton_Checked"  />
                            <RadioButton      Style="{StaticResource MaterialDesignTabRadioButton}" Margin="4" IsChecked="False" Content="设置"  
                                             Tag="2" Checked="RadioButton_Checked" />
    
                        </StackPanel>
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22">文件分类浏览器</TextBlock>
                    </DockPanel>
                </materialDesign:ColorZone>
                <ContentControl Content="{Binding MainPanel}"/>
            
            </DockPanel>
        </materialDesign:DrawerHost>

        <local:InputDialog x:Name="inputDialog" Grid.RowSpan="999" Grid.ColumnSpan="999"  Margin="-4" Width="{ Binding ElementName=win, Path=ActualWidth }"/>
        <local:ProgressDialog x:Name="progressDialog" Grid.RowSpan="999" Grid.ColumnSpan="999"  Margin="-4" Width="{ Binding ElementName=win, Path=ActualWidth }"/>
        <local:AboutDialog  x:Name="aboutDialog" Grid.RowSpan="999" Grid.ColumnSpan="999"  Margin="-4" Width="{ Binding ElementName=win, Path=ActualWidth }"/>
        <materialDesign:Snackbar x:Name="snack">
            <materialDesign:SnackbarMessage   />
        </materialDesign:Snackbar>
    </Grid>
</local:WindowBase>
